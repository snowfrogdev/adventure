import { Room } from "../room";
import { myRoom } from "./my-room";
import { corridor } from "./corridor";
import { lobby } from "./lobby";


export const rooms = [
    myRoom,
    corridor,
    lobby
]
